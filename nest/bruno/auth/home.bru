meta {
  name: home
  type: http
  seq: 2
}

get {
  url: {{HOST}}
  body: none
  auth: none
}

tests {
  test("should login request OK", function () {
    expect(res.getStatus()).to.equal(200);
  });
   
}

settings {
  encodeUrl: true
}
